type User {
  id: ID!
  email: String!
  password: String!
}

type Institution {
  id: ID!
  title: String!
  description: String!
  administrator: User! @belongsTo
}

type Intership {
  id: ID!
  title: String!
  description: String!
  institution: Institution! @belongsTo
}

type Application {
  id: ID!
  cv: String!
  email: String!
  intership: Intership! @belongsTo
}

type Query {
  currentUser: User
}

type Mutation @group(middleware: ["auth:api"]) {
  signOut: User!
}

extend type Mutation {
  signUp(email: String!, password: String!): User!
  signIn(email: String!, password: String!): User!
}